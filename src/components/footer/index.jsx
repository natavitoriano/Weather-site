import React from "react";

import { CustomFooter } from "./footer";

//this component is the footer
const Footer = props => (
    <footer>
        <CustomFooter flex='row' margin="10px">
        </CustomFooter>
    </footer>
)

export default Footer;